server:
    host: openl10n.dev
    login: user
    password: user

project: foobar

files:
    expr: src/Blablacar/Bundle/*/Resources/translations/{domain}.{local}.*
    expr: strings/{locale}/LC_MESSAGES/{domain}.*
    exclude: countries.en.yml

options: ~
